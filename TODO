- Instruction fetch/decode/execute
- PPU rendering
  (system rendering interface)
- memory mapped IO for reading input state
  (system input interface)
- Test rom execution
- Simulate frequency of cpu execution
- Bus read/write should handle all peripherals (only carts and some memory
  registers are currently supported)